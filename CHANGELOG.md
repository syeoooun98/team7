# CHANGELOG

## 2026-07-15 (2) — 채용자 진단 탭 대개편 + soul 브랜치에 대시보드 첫 반영

이전 `soul` 브랜치에는 DB.md 수정만 있었고 대시보드 코드(index.html/css/js)는 없었습니다(1차 MVP는 "soul로만 푸시" 규칙이 생기기 전에 main에 직접 커밋됨). 이번 푸시로 soul에도 대시보드 전체가 처음 올라가며, 아래는 main 최초 커밋 이후 로컬에서 누적된 변경사항입니다.

### 채용자 입장 — 1. 공고 경쟁력 진단
- **보상금 백분위**: `category_market_stats` 3점(평균/P50/P90) 선형보간 근사 → 실제 로드된 `positions` 데이터로 동일 직군 내 정확한 순위를 계산하는 방식으로 교체
- **[제거]** 회사 배지 기반 "부족한 매력 태그 제안", `tag_effect_stats` 기반 "매력 태그별 지원자수·합격률 비교" — 창작된 수치를 실측처럼 보여줄 위험이 있어 제거
- **[신규] 배지 획득 챌린지**(실측): `category_market_stats`의 업계 평균/상위 10% 보상금 대비, 이 공고가 도달하기까지 남은 금액만 표시(전환율 예측 없음)
- **[신규] 회사 배지 벤치마킹**(실측): `company_tags`(원티드가 실제 부여한 회사 배지) 기준, 동일 직군 회사들의 배지 보유율과 우리 회사 보유 현황 비교
- **[신규] 매력 태그 효과 시뮬레이션**(데모): `tag_effect_stats` 전량(87/87)이 합성값임을 명시하는 배너·라벨과 함께, 선택과 무관한 고정 참고 섹션으로 분리 배치
- **[신규] 경쟁 공고 한눈에 보기**(실측): 같은 직군에서 경쟁하는 타사의 현재 오픈 공고를 보상금 높은순으로 표시
- **회사/공고 선택**: `<select>` 드롭다운 → 검색 가능한 콤보박스로 교체(회사명/공고명 입력 필터링), 공고 선택은 직무(직군)별로 그룹핑
- **레이아웃**: 선택에 따라 함께 바뀌는 4개 카드(경쟁 공고 → 보상금 백분위 → 배지 획득 챌린지 → 회사 배지 벤치마킹)를 순서대로 배치하고, 선택과 무관한 매력 태그 데모 섹션은 별도 배치

### 탭 구조 정리
- 지원자 "2. 기업 건강도 비교" 탭 제거 (`company_insight_snapshots` 미확보로 무기한 차단)
- 채용자 "2. 파이프라인 위험 알림" 탭 제거 (ATS 키 미확보로 무기한 차단)

### 문서
- README.md: 위 변경사항 반영해 구현 범위 표·근사치 설명 갱신

## 2026-07-15 (1) — DB.md: company_insight_snapshots 차단 사유 정정

v1 `/insight/company`를 등록번호 보유 회사 2,437건 전량 실제 호출해 검증. 같은 공용 client-id/secret으로 다른 v1 엔드포인트(`/jobs/{id}`)는 200 정상이지만 이 엔드포인트만 전량 401 `"No permission -- see authorization schemes"` 반환 — 키 문제가 아니라 이 엔드포인트에 대한 별도 권한 승인이 필요한 상태임을 DB.md 8·9장에 반영.
