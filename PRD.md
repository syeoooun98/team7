# Wanted 통합 채용 대시보드 PRD

## 1. 개요

원티드 OpenAPI v1(기업/ATS용)·v2(공개 채용공고 검색) 스펙을 기반으로, 지원자와 채용 담당자가 각자의 관점에서 활용할 수 있는 채용 데이터 대시보드를 만든다. 상단 탭에서 "지원자 입장 / 채용자 입장"을 전환하며, 각 입장마다 목적이 다른 하위 대시보드를 제공한다.

## 2. 배경 및 문제의식

- 구직자는 공고를 검색만 할 뿐, 시장 전체 흐름·기업 건강도·본인 스펙과의 격차·본인 우선순위를 종합적으로 볼 도구가 없다.
- 채용 담당자는 지원 현황을 사후적으로 집계할 뿐, 지금 무엇을 조치해야 하는지, 어떤 공고가 제일 급한지, 처리 과정에 형평성 문제는 없는지를 한눈에 보여주는 도구가 없다.
- 두 입장 모두 "데이터를 보여주는 것"에서 그치지 않고 "다음 행동"으로 이어지는 것을 목표로 한다.
- 일부 데이터(스킬 태그 트렌드 등)는 지원자·채용자 양쪽 모두에게 유용하므로, 같은 데이터 소스를 서로 다른 UI로 재사용하는 것을 전제로 설계한다.

## 3. 타겟 사용자

| 구분 | 설명 |
|---|---|
| 지원자(구직자) | 취업/이직을 준비 중이며, 시장 동향 파악부터 실제 지원까지 한 곳에서 하고 싶은 사용자 |
| 채용 담당자 | 원티드에 공고를 올리고 지원자를 관리하는 기업 채용 담당자 |

## 4. 정보 구조 (IA)

```
Wanted 통합 채용 대시보드
├─ 지원자 입장
│   ├─ 0. 마켓 홈 (Zoom-out → Zoom-in → Action)   ← 신규, 기본 진입 탭
│   ├─ 1. 취준 로드맵
│   ├─ 2. 기업 재무·인력 건강도 비교
│   ├─ 3. 우대조건 특화 필터
│   └─ 4. 개인 맞춤형 공고 재정렬
└─ 채용자 입장
    ├─ 1. 공고 경쟁력 진단
    ├─ 2. 파이프라인 병목·이탈 위험 알림 (목표달성 스캔바 + 형평성 알림 포함, 확장)
    └─ 3. 채용 조건 트렌드 (태그 기반)   ← 신규
```

마켓 홈은 지원자가 가장 먼저 보는 화면으로, Action 단계의 추천 카드는 "개인 맞춤형 공고 재정렬" 탭의 우선순위 설정을 그대로 재사용한다. 채용 조건 트렌드는 지원자·채용자 양쪽에서 같은 데이터를 다른 화면으로 소비하는 공용 모듈로 취급한다(6장 참조).

## 5. 기능 요구사항

### 5.0 마켓 홈 (신규)

**목적**: 지원자가 진입 직후 시장 전체 상황을 스캔하고, 최근 변화를 파악한 뒤, 바로 지원 행동으로 넘어가게 한다.

**A. 전체 시장 판세 (Zoom-out)**
- 직군별 공고 분포 차트: 개발/경영·비즈니스/디자인/마케팅/데이터/영업 등 주요 직군별 현재 오픈된 공고 수와 전체 대비 비율(%)을 바/도넛 차트로 표시
- 지역별 채용 집중도 차트: IT·스타트업 밀집 지역(강남, 판교, 성수 등) 기준 공고 수를 내림차순 정렬해 표시
- 데이터 소스: v2 `/jobs` 목록 응답의 `category_tags`(parent_tag/child_tags), `address` 집계 (`subcategory_tags`/`locations`는 조회 시 넘기는 필터 파라미터명이며, 실제 응답 필드명과는 다름)

**B. 이번 주 핵심 변화 (Zoom-in, 최근 7일)**
- 이번 주 신규 등록 공고 수 + 전주 대비 증감률(%), 이번 주 마감 종료 공고 수
- 최근 7일 총 공고 수 추이 미니 라인 차트
- 급등 직무 TOP3 / 급감 직무 TOP3 랭킹 (직무·포지션 단위)
- 참고: 스킬 태그 단위의 상승/하락 트렌드는 5.7 "채용 조건 트렌드" 모듈 데이터를 요약 인용해 보여줄 수 있음 (직무 단위 트렌드와 스킬 단위 트렌드는 서로 다른 축)
- 데이터 소스: v2 `/jobs`를 일 단위로 수집해 자체 적재한 시계열 스냅샷 (※ 7장 제약사항 참조 — API가 과거 시점 데이터를 직접 제공하지 않음)

**C. 지원할 공고 (Action)**
- 추천 공고 카드: 기업 로고(`logo_img`), 회사명, 포지션명, 핵심 기술스택 태그(`skill_tags`), 보상금(`reward.total`)을 콤팩트하게 표시. 정렬 기준은 "개인 맞춤형 공고 재정렬" 탭의 우선순위 설정값을 재사용
  - ⚠️ `skill_tags`는 v2 `/jobs` 목록 응답엔 없고 **v1 `GET /jobs/{job_id}` 상세조회**에만 존재한다. 카드에 노출하려면 추천 후보 공고 각각에 대해 상세조회를 추가로 호출해야 한다(전수 호출이 아니라 카드에 뜨는 소수 후보에 한해서만 호출하는 것을 전제로 설계).
- 마감 임박 긴급 공고 영역: `due_time` 기준 D-day가 임박한 공고를 붉은 뱃지("D-2 마감임박!")로 강조, 우측 "공고 보기" 버튼으로 즉시 지원 유도

### 5.1 취준 로드맵

- 입력: 희망 직군, 현재 경력 연차, 보유 스킬(체크박스)
- 출력: 직군별 평균 요구 연차·연봉 대비 내 스킬 커버리지(%), 경력 갭, 요구빈도 높은 미보유 스킬 TOP3, 현재 스펙으로 지원 시 유리한 공고 TOP3
- 데이터 소스: v2 `category_tags`/`years`는 목록 조회로 바로 집계 가능. `skill_tags`는 v2 목록엔 없어 **v1 `/jobs/{job_id}` 상세조회를 공고별로 추가 호출**해 자체 적재한 뒤 집계해야 한다(7장 참조). 5.7의 "상승 스킬 태그" 데이터를 "지금 배우면 좋은 스킬" 추천에 가중치로 반영 가능

### 5.2 기업 재무·인력 건강도 비교

- 입력: 비교할 관심 기업 최대 4곳 선택
- 출력: 레이더 차트(연봉수준/안정성/성장성/업력/생산성), 세부 지표 테이블(평균연봉, 퇴사율, 인원변동율, 업력, 1인당 매출)
- 데이터 소스: v1 `/insight/company` (`averageSalary`, `leftRate`, `employeeChangeRate`, `age`, `salesPerPerson` 등)

### 5.3 우대조건 특화 필터

- 입력: 외국인 우대 / 병역특례 / 장애인 우대 중 해당 조건 체크
- 출력: 선택한 조건에 해당하는 공고만 필터링한 리스트
- 데이터 소스: v2 `additional_apply_types`

### 5.4 개인 맞춤형 공고 재정렬

- 입력: 직종/연봉/워라밸/출퇴근거리 각각에 대해 "포기불가" 여부 + 중요도(0~5)
- 로직: 포기불가로 표시된 조건은 하드 필터로 작동(미달 공고 제외), 나머지는 가중치 기반 매칭 점수로 정렬
- 출력: 종합 매칭 점수, 조건별 매칭률 칩, 하드필터로 제외된 공고 수 안내
- 데이터 소스: v2 `category_tags`, 회사 평균연봉(v1 `/insight/company`, 근사), 좌표 기반 근사 거리(v1 `/jobs/{job_id}`의 `address.geo_location` 우선 사용, 비어있는 공고는 카카오 로컬 API로 지오코딩 보완)
  - ⚠️ `attraction_tags`(워라밸 근사)는 공고 단위로 값을 주는 필드가 API에 없다(마스터 목록 `/tags/attractions`와 v2 검색 필터로만 존재). "이 공고가 재택근무 태그를 갖고 있는지"는 각 매력태그ID로 v2 `/jobs?attraction_tags=X`를 호출해 결과에 포함되는지로 역으로 알아내야 하며(태그 수만큼 N회 호출), 워라밸 조건의 하드필터/가중치 매칭은 이 방식이 실제로 구현 가능한지 먼저 검증이 필요하다. 검증 전까지는 워라밸 조건을 MVP에서 제외하거나 "일부 매력 태그만 근사 지원"으로 범위를 축소하는 것을 고려한다.

### 5.5 공고 경쟁력 진단 (채용자)

- 입력: 진단할 자사 공고 선택
- 출력: 동일 직군 시장 대비 보상금 백분위 게이지, 부족한 매력 태그 제안("재택근무 태그를 추가하면 노출 경쟁력 개선"), 매력 태그별 평균 지원자수/서류합격률 비교
- 데이터 소스: v2 `reward` 시장 집계 + 자사 ATS 지원 성과 데이터
  - ⚠️ "부족한 매력 태그 제안", "매력 태그별 지원자수/합격률 비교"는 특정 공고에 어떤 매력 태그가 붙어있는지를 전제로 하는데, ATS 공고 관리 API(`ATSPositionCreateSerializer`/`ATSPositionSerializer` 등)에도 매력 태그 필드가 없다. 5.4와 동일한 문제이며, 이 기능은 "필터 역질의로 태그 보유 여부를 근사"하는 방식의 실현 가능성 검증 결과에 따라 MVP 범위·정확도가 달라진다.

### 5.6 파이프라인 병목·이탈 위험 알림 (채용자, 확장)

**목표 달성 예측 스캔바 (신규, 최상단 요약 레이어)**
- 진행 중인 공고별로 진행률 바 하나로 상태를 표시: 미달 위험(빨강, 목표 대비 지원자 확보율 낮음) / 지켜봐야 함(주황, 경계선) / 정상 진행(초록, 목표 확보 예상)
- 목적: 카드를 하나씩 읽지 않고도 "지금 어느 공고가 제일 급한지" 색과 길이만으로 즉시 스캔. 클릭 시 아래 상세 위험 카드로 이동
- 데이터 소스: 공고별 목표 지원자 수(설정값) 대비 현재 지원자 수, 마감까지 남은 기간

**위험 신호 카드 (기존 + 신규 유형 추가)**
- 마감임박 + 지원자 부족 → 액션: 마감일 연장 / 보상금 상향 / 조건 완화(지역·연차) / 매력 태그 추가
- 서류합격 후 정체 → 액션: 담당자 리마인드 / SLA 기준 설정
- 초기 미열람 → 액션: 지연 안내 메시지 발송 / 우선 열람 큐 등록
- **(신규) 처리 지연 알림**: 이력서 열람 후 N일 이상 상태 변경 없음 → 액션: 담당자 리마인드, 상태 업데이트 촉구
- **(신규) 형평성 알림**: 동일 공고 내 지원자 간 처리 소요 시간 편차가 비정상적으로 큼(예: 4배 이상) → 액션: 처리 기준 재점검 권고, 정체된 지원자 우선 검토 유도
- 데이터 소스: v1 `due_time`, `apply_time`/`open_time`, `ATSApplicationDetailSerializer.status`, 지원자별 처리 소요시간 분산
  - ⚠️ API는 지원서의 **현재 상태값 하나만** 제공하며, 상태가 언제 바뀌었는지에 대한 이력(타임라인) 필드는 없다. "정체", "처리 지연", "형평성" 판단에 필요한 상태 변경 이력은 **우리 시스템이 주기적으로(예: 1시간 간격) `status`를 폴링해서 값이 바뀐 순간을 직접 감지·기록**해 만들어야 한다. 폴링 주기보다 짧게 여러 번 상태가 바뀌면 중간 전이를 놓칠 수 있다는 한계를 인지한다.

### 5.7 채용 조건 트렌드 (태그 기반, 신규, 채용자)

- 입력: 없음(전사 또는 자사 공고 기준 자동 집계), 필요 시 직군 필터
- 출력: 전분기 대비 상승 스킬 태그 TOP3(예: AI 활용 경험 +41%, Kubernetes +18%, Python +9%), 하락 스킬 태그 TOP3(예: jQuery -22%, PHP -14%, Flash -8%)
- 용도: 시장에서 보도되는 트렌드(AI 채용 증가 등)가 실제로 우리 회사 공고 태그에도 반영되고 있는지 검증, 다음 공고 작성 시 어떤 스킬/태그를 강조할지 결정
- 공용 데이터 모듈 메모: 이 집계는 지원자용 화면(마켓 홈 Zoom-in 참고 정보, 취준 로드맵의 "지금 배우면 좋은 스킬" 추천)에도 동일 소스로 재사용 가능 — 같은 데이터, 다른 UI
- 데이터 소스: 공고 등록 시 누적되는 `skill_tags`의 시계열 집계 (1단계 버전은 태그 카운트 기반, 실제 유의미하려면 누적 기간 필요)
  - ⚠️ `skill_tags`는 v2 목록에 없으므로, 일 배치가 v2로 공고 목록을 받은 뒤 (신규/갱신된 공고에 한해) v1 `/jobs/{job_id}` 상세조회를 추가로 호출해 스킬 태그를 채워야 한다. 전체 공고를 매일 상세조회하는 것은 호출량이 커지므로, 신규 등록/상태 변경된 공고만 상세조회하는 증분 방식을 전제로 한다.

## 6. 공용 데이터 모듈 — 스킬 태그 트렌드

| 소비 주체 | 화면 | 활용 방식 |
|---|---|---|
| 채용 담당자 | 5.7 채용 조건 트렌드 | 자사 공고가 시장 트렌드를 반영하는지 검증, 태그 전략 결정 |
| 지원자 | 5.0 마켓 홈 Zoom-in | "요즘 뜨는 스킬/직무" 참고 정보로 요약 노출 |
| 지원자 | 5.1 취준 로드맵 | 미보유 스킬 추천 시 상승 트렌드 태그에 가중치 반영 |

동일한 스킬 태그 시계열 집계 배치 작업 하나로 위 세 화면을 모두 지원하는 것을 전제로 한다.

## 7. 데이터 소스 매핑 요약

| 요소 | API 필드 | 비고 |
|---|---|---|
| 직군/직무 | v2 `/jobs` 응답의 `category_tags`(parent_tag/child_tags) | 목록 응답에 바로 포함, 실시간 집계 가능 |
| 지역 | v2 `/jobs` 응답의 `address`(country/location/full_location) | 목록 응답에 바로 포함, 실시간 집계 가능 |
| 스킬 태그 | v1 `/jobs/{job_id}` 상세조회의 `skill_tags` | ⚠️ v2 목록엔 없음. 공고별 상세조회를 추가 호출해야 얻어짐(N+1) |
| 워라밸 근사 | ⚠️ 공고 단위 필드 없음. `/tags/attractions` 마스터 목록 + v2 검색 필터(`attraction_tags`)로만 존재 | "이 공고에 어떤 매력태그가 있는지"는 API가 직접 주지 않음. 필터 역질의(태그별 N회 호출)로만 근사 가능 — 실현 가능성 검증 필요 |
| 연봉 | 공고 단위 필드 없음 → v1 `/insight/company`의 `InsightCompanyDetailResponseSerializer.averageSalary` | 회사 단위 근사치, 포지션 단위 아님. 실제 스키마 확인 완료 |
| 출퇴근거리 | v1 `/jobs/{job_id}` 상세조회의 `address.geo_location` | ⚠️ v2 목록 주소엔 좌표 없음. `geo_location`은 스펙상 `type: object`로만 정의돼 내부 키(위경도 이름)는 실제 호출 후 확인 필요. 비어있는 공고는 카카오 로컬 API(`Map_API_KEY`)로 `full_location` 지오코딩 |
| 보상금 | `reward.total`/`recommender`/`recommendee` | 추천 보상금(급여 아님) |
| 우대조건 | `additional_apply_type` | 외국인/병역특례/장애인. v2 목록 응답에 포함 |
| 지원 현황 | `ApplicationSummaryResponseSerializer`, `ATSApplicationDetailSerializer` | v1 ATS. 공용 Client ID/Secret 외에 기업별 `X-Wanted-Dashboard-Service-Key` 추가 필요(8장 참조) |
| 처리 소요시간/형평성 | `apply_time`, `open_time` | ⚠️ "지원자별 상태 변경 이력"은 API 필드가 아님 — 현재 `status` 한 값만 제공되므로, 우리가 주기적 폴링으로 상태 변화를 직접 감지·기록해야 함 |
| 스킬 태그 트렌드 | `skill_tags` 시계열 누적 | v1 상세조회 기반. 자체 배치 집계 필요 (7.1 제약 참조) |
| 최근 7일 트렌드 | 없음(스냅샷 API) | 자체 일 배치 수집·적재 필요 |

## 8. 비기능 요구사항 및 제약사항

- API 키는 클라이언트(브라우저) 코드에 절대 포함하지 않는다. 실제 연동 시 백엔드에서 키를 보관하고 프록시하는 구조로 구현한다.
- "최근 7일 변화", "채용 조건 트렌드" 관련 위젯은 원티드 API가 시계열/히스토리를 제공하지 않으므로, 별도 배치 작업으로 일 단위 공고·태그 스냅샷을 수집·저장하는 파이프라인이 선행되어야 한다.
- 연봉(포지션 단위)과 출퇴근 소요시간은 원티드 API만으로 정확히 계산할 수 없는 항목이므로, UI 상에 "근사치"임을 명시한다.
- 형평성 알림은 통계적으로 "비정상"을 판단하는 기준(예: 동일 공고 내 처리시간 표준편차·배수 임계값)을 별도로 정의해야 하며, 표본이 적은 공고에는 오탐 가능성이 있어 최소 지원자 수 조건을 둔다.
- 현재 제공된 데모(`unified-dashboard-demo.html`)는 목데이터 기반이며 실제 API 연동이 아니다.
- v1 ATS 관련 화면(5.6, 지원서 상태/처리시간)은 공용 Client ID/Secret 외에 기업별로 별도 발급받는 `X-Wanted-Dashboard-Service-Key`가 있어야 실제 연동이 가능하다. 이 키가 없는 동안은 해당 화면을 목데이터로만 시연한다.
- 워라밸 근사(`attraction_tags` 공고 단위 매칭)는 API가 직접 제공하지 않아 필터 역질의 방식의 실현 가능성을 먼저 검증해야 하며, 검증 실패 시 5.4의 워라밸 조건은 MVP 범위에서 제외될 수 있다.

## 9. 향후 확장 (Out of scope for MVP)

- 실제 원티드 API 연동을 위한 백엔드 프록시 서버 구축
- 출퇴근 소요시간 계산을 위한 외부 지도/길찾기 API 연동
- 자소서 합격 예측 AI(`/ai/apply/text-prediction/async`)를 활용한 자소서 첨삭 기능
- 채용 담당자용 지원자 자동 스크리닝 랭킹 대시보드(지원자 우선순위 매칭의 대칭 기능)
- 스킬 태그 트렌드의 구직자 공개 페이지(별도 UI) 분리 제공

## 10. 성공 지표 (제안)

- 지원자: 마켓 홈 → 세부 도구 → 지원 완료로 이어지는 전환율
- 채용자: 위험 알림/스캔바 노출 후 액션 버튼 클릭률, 액션 적용 후 실제 지원자 수·처리 속도 개선 여부, 형평성 알림 발생 건수 감소 추이
